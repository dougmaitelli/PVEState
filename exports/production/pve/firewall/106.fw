[OPTIONS]

log_level_in: nolog
enable: 1

[RULES]

IN ACCEPT -p tcp -dport 8554 -log nolog # RTSP
IN ACCEPT -p tcp -dport 8555 -log nolog # WebRTC
IN ACCEPT -p udp -dport 8555 -log nolog # WebRTC
IN ACCEPT -i net1 -p tcp -dport 2375 -log nolog # Docker API

