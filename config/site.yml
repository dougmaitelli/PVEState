site:
  name: h4des
  environment: production

proxmox:
  endpoint: https://pve.h4des.dev:8006
  existing_environment: true
  change_policy: manual_only

workload_profile:
  virtual_machines: 1
  containers: 10+

backup:
  pbs:
    deployment: inside_proxmox
    endpoint: pending_discovery
  s3:
    configured_in_pbs: true
    endpoint: intentionally_not_recorded_until_reviewed
