schema_version: 1
cluster:
  enabled: true
  log_level_in: info
  rules:
    - {direction: IN, action: ACCEPT, interface: vmbr0, protocol: tcp, destination_port: "8006", log: nolog, comment: Proxmox UI}
nodes:
  pve:
    present: true
    rules:
      - {direction: IN, action: ACCEPT, interface: vmbr0, protocol: tcp, destination_port: "22", log: nolog, comment: SSH administration}
guests:
  101:
    enabled: true
    log_level_in: info
    rules:
      - {direction: IN, action: ACCEPT, interface: net0, protocol: tcp, destination_port: "443", log: nolog, comment: HTTPS applications}
absent_guest_files: []
