---
all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    proxmox:
      hosts: {}
    lxcs:
      hosts: {}
    vms:
      hosts: {}

# This stays empty until API discovery has been reviewed. Existing production
# guests must not be inferred into managed state without explicit approval.
